<UserControl x:Class="Imagio.Helpdesk.View.Entity.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0" Content="Фамилия" />
        <TextBox Grid.Column="1" Grid.Row="0" VerticalAlignment="Center" Text="{Binding Path=Model.LastName, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />
        <Label Grid.Column="0" Grid.Row="1" Content="Имя" />
        <TextBox Grid.Column="1" Grid.Row="1" VerticalAlignment="Center" Text="{Binding Path=Model.FirstName, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />
        <Label Grid.Column="0" Grid.Row="2" Content="Отчество" />
        <TextBox Grid.Column="1" Grid.Row="2" VerticalAlignment="Center" Text="{Binding Path=Model.SecondName, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}" />
    </Grid>
    
</UserControl>
