<UserControl x:Class="Imagio.Helpdesk.View.Workspace.AccountWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <DataGrid Style="{StaticResource ResourceKey=EntityWorkspaceGrid}">
        <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding Path=Login}" Header="Имя пользователя" />
            <DataGridTextColumn Binding="{Binding Path=PasswordString}" Header="Пароль" />
            <DataGridCheckBoxColumn Binding="{Binding Path=IsActive}" Header="Активный" />
        </DataGrid.Columns>
    </DataGrid>

</UserControl>
