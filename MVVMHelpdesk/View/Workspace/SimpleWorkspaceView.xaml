<UserControl x:Class="Imagio.Helpdesk.View.Workspace.SimpleWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataGrid ItemsSource="{Binding Path=ItemColletion}" 
              SelectedItem="{Binding Path=SelectedItem}"
              AutoGenerateColumns="False"
              IsReadOnly="True">
        <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding Path=Id}" Header="Идентификатор" />
            <DataGridTextColumn Binding="{Binding Path=Name}" Header="Наименование" />
        </DataGrid.Columns>
    </DataGrid>

</UserControl>
